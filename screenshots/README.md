# Demo Asset Notes

These files are reproducible output artifacts captured from the app’s validated deterministic Apple demo path. They are not fake UI screenshots and they do not claim live market data.

## Included artifacts

- openapi_snapshot.json — a compact OpenAPI evidence snapshot for the live API surface
- apple_company_resolution.json — validated Apple company-resolution payload
- apple_synthesis.json — validated Apple synthesis payload
- validation_error.json — deterministic validation error sample

These artifacts are intended for portfolio/demo packaging and should be treated as evidence of the current app behavior, not as claims of active external-market feeds.
